export type Tab = "image" | "video" | "config" | "toolbox" | "history";

export const TAB_LABEL: Record<Tab, string> = {
  image: "图片生成",
  video: "视频生成",
  config: "配置",
  toolbox: "工具箱",
  history: "历史"
};
